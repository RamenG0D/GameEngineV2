package Renders;

import java.awt.Graphics;

public interface Camera { // basic camera class to define basic camera methods so they can also be user defined
    //
    public void render(Graphics g);
    //
}
